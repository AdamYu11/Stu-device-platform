{
  "name": "device-server",
  "version": "1.0.0",
  "private": true,
  "description": "设备测试平台后端：测试数据 + 手机参数录入 API",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "dev": "node --env-file-if-exists=.env --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^2.2.0"
  }
}
